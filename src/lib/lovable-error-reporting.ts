// Error reporting stub — no external service dependency.
// Exported to maintain API compatibility with imports across the codebase.
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function reportLovableError(_error: unknown, _context: Record<string, unknown> = {}): void {
  // no-op: error reporting removed
}
